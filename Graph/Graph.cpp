#include <iostream>
#include "Graph.h"

using namespace std;

int main()
{

Graph::Graph(int n)
{
    vector<char> color[0] = '\o';
    vector<int> parent[0] = NULL;
    vector<int> distance[0] = NULL;
    vector<List<int> > adj[0] = NULL;
    for(i=1; i <n+1; i++)
    {
        vector<char> color[i] = W;
        vector<int> parent[i] = 0;
        vector<int> distance[i] = -1;
    }
};

Graph::~Graph()
{
    while()
}
















    return 0;
}
